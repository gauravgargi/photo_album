<div class='header clearfix'>
  <ul>
    <li><%= link_to t('home'), root_path %></li>
    <% if user_signed_in? %>
      <li><%= link_to t('my_albums'), albums_path %></li>
      <li><%= link_to t('settings'), edit_registration_path(:user) %></li>
      <li><%= link_to t('signout'), destroy_user_session_path, :method => :delete %></li>
    <% else %>
      <li><%= link_to t('login'), new_session_path(:user) %></li>
    <% end %>
  </ul>
</div>